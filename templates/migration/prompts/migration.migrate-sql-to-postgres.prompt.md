---
description: 將 SQL Server schema 或 SQL 邏輯改寫為 PostgreSQL 版本。
agent: migration.db-architect
argument-hint: sql file, stored procedure, query block, or schema object
---

Review the selected SQL Server database artifact and rewrite or redesign it for PostgreSQL.

Output:
- original intent
- SQL Server specific dependencies
- PostgreSQL rewrite
- semantic differences
- data correctness risks
- validation queries
